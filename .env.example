# ── Next.js (artifacts/daddyx) ───────────────────────────────────────────────
# Port for the dev server. Defaults to 3000 when unset.
PORT=3000

# PostgreSQL connection string (required by the app and DB library).
DATABASE_URL=postgresql://postgres:password@localhost:5432/daddyx
